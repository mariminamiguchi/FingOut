<% if logged_in? %>
  <% if current_user.liking?(plan) %>
    <%= form_with(model: current_user.favorites.find_by(plan_id: plan.id), local: true, method: :delete) do |f| %>
      <%= hidden_field_tag :plan_id, plan.id %>
      <%= f.submit 'お気に入り削除', class: 'btn btn-outline-danger' %>
    <% end %>
  <% else %>
    <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
      <%= hidden_field_tag :plan_id, plan.id %>
      <%= f.submit 'お気に入り追加', class: 'btn btn--outline-secondary' %>
    <% end %>
  <% end %>
 <% else %>
   <%= link_to 'お気に入り追加', signup_path, class: 'btn btn-outline-secondary' %>
  <% end %>